image: node:18-alpine

variables:
  NPM_CONFIG_JOBS: 1

stages:
  - build

build-backend:
  stage: build
  script:
    - apk add --no-cache python3 make g++ 
    - npm install --no-optional
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 week